# Copy this file to create your local .env.docker (do NOT check that file into Git)

# PostgreSQL
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change_me_postgres
POSTGRES_DB=pandawa

# pgAdmin
PGADMIN_DEFAULT_EMAIL=ortala@dpd.go.id
PGADMIN_DEFAULT_PASSWORD=change_me_pgadmin

# App / other secrets (examples)
#JWT_SECRET=replace_with_secure_random_value
#ANOTHER_SECRET=...
